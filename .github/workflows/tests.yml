name: "Tests"

on:
  push:
    branches:
      - main
  pull_request:
    types: [ opened, reopened, ready_for_review, synchronize ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    # For pipefail - https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#exit-codes-and-error-action-preference
    shell: bash

jobs:
  linters:
    name: linters
    runs-on: [ self-hosted, macos, x64, mac, m3max ]

    steps:
      - uses: actions/checkout@v6
      - id: setup
        uses: ./.github/actions/setup
        with:
          linters: true

      - name: Check Rust formatting
        if: ${{ always() && steps.setup.outcome == 'success' }}
        run: cargo fmt --check

      - name: Check Metal formatting
        if: ${{ always() && steps.setup.outcome == 'success' }}
        run: ./scripts/clang-format.sh --check

      - name: Check that Cargo.lock is consistent with Cargo.toml
        if: ${{ always() && steps.setup.outcome == 'success' }}
        run: |
          cargo metadata --locked --format-version=1 > /dev/null
          git diff --exit-code -- Cargo.lock

  tests:
    name: ${{ matrix.backend }} ${{ matrix.tracing && ' (tracing)' || '' }} ${{ matrix.audio && ' (audio-runtime)' || '' }}
    runs-on: [ self-hosted, macos, x64, mac, m3max, runner1 ]
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.draft == false }}

    strategy:
      matrix:
        backend: [ 'cpu', 'metal' ]
        tracing: [ false, true ]
        audio: [ false, true ]

    env:
      CARGO_ENCODED_RUSTFLAGS: "-Dwarnings"

    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup
        with:
          metal: ${{ matrix.backend == 'metal' }}
          test_model: true
          nextest: true

      - name: Build crates
        run: |
          cargo +stable build --no-default-features \
            ${{ matrix.backend != 'cpu' && format('--features {0}', matrix.backend) || '' }} \
            ${{ matrix.tracing && '--features tracing' || '' }} \
            ${{ matrix.audio && '--features audio-runtime' || '' }}

      - name: Run tests
        if: ${{ matrix.backend != 'cpu' && matrix.tracing && matrix.audio }}
        run: |
          cargo +stable nextest run --profile=ci --no-default-features \
            ${{ format('--features {0}', matrix.backend) || '' }} \
            --features tracing --features audio-runtime
