{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Format Code",
      "type": "shell",
      "command": "cargo +nightly fmt && ((command -v metal-analyzer >/dev/null 2>&1 && metal-analyzer format) || ./scripts/clang-format.sh)",
      "problemMatcher": []
    }
  ]
}
