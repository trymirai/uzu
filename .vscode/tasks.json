{
  "version": "2.0.0",
  "inputs": [
    {
      "id": "modelRepo",
      "type": "command",
      "command": "shellCommand.execute",
      "args": {
        "command": "uv run downloader list --repos",
        "cwd": "${workspaceFolder}/tools"
      }
    }
  ],
  "tasks": [
    {
      "label": "Format Code",
      "type": "shell",
      "command": "cargo +nightly fmt && ./scripts/clang-format.sh",
      "problemMatcher": []
    },
    {
      "label": "Prepare Bindings",
      "type": "shell",
      "command": "cargo run -p cli-release prepare-bindings",
      "problemMatcher": []
    },
    {
      "label": "Prepare Workspace",
      "type": "shell",
      "command": "cargo run -p cli-release prepare-workspace \"$(uv run python3 -c 'import tomllib; print(tomllib.load(open(\"Cargo.toml\", \"rb\"))[\"workspace\"][\"package\"][\"version\"])')\"",
      "problemMatcher": []
    },
    {
      "label": "Download Model",
      "type": "shell",
      "command": "uv run downloader download \"${input:modelRepo}\"",
      "options": {
        "cwd": "${workspaceFolder}/tools"
      },
      "problemMatcher": []
    }
  ]
}