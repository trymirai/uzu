if [[ -z "${USE_HOST_XCODE:-}" ]]; then
    if xcrun metal --version >/dev/null 2>&1; then
        USE_HOST_XCODE=1
    else
        USE_HOST_XCODE=0
    fi
fi

if [[ "$USE_HOST_XCODE" == "1" ]]; then
    FLAKE_REF=".#host-xcode"
else
    FLAKE_REF="."
fi

use flake "$FLAKE_REF"
